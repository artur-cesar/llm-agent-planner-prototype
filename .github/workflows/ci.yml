name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  health-check:
    name: Health Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
