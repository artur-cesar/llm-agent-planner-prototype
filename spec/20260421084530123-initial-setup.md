Context:
- This is a fresh NestJS backend project.
- The project is intended to evolve into a study prototype for LLM-based systems (e.g., agents, planners, tool calling).
- At this stage, the application has no domain logic, no business rules, and no external integrations.
- The goal of this step is to establish a solid and predictable project foundation before adding any application-specific features.
- The project should be ready for iterative development using spec-driven workflows.

Goal:
- Bootstrap a clean, stable, and reproducible NestJS project foundation.
- Pin the Node.js version to ensure consistent behavior across environments.
- Set up environment configuration files.
- Ensure basic project hygiene (gitignore, editorconfig, scripts, README).
- Prepare the repository for future specs without introducing any business logic.

Non-goals:
- Do not implement any domain logic
- Do not introduce LLM integration
- Do not create any endpoints beyond the default NestJS scaffold
- Do not introduce database integration
- Do not introduce Docker or CI yet
- Do not add Swagger/OpenAPI
- Do not add linting or formatting tools beyond what NestJS already provides by default
- Do not overengineer configuration or folder structure

Constraints:
- Use NestJS CLI to generate the project
- Keep everything simple and minimal
- Avoid adding unnecessary dependencies
- Ensure all configuration is explicit and easy to understand
- Favor reproducibility and clarity over flexibility at this stage

Behavior compatibility:
- The application must start successfully using the default NestJS bootstrap
- Existing NestJS default behavior must remain intact
- No changes should break the default `/` endpoint provided by NestJS (if present)
- The project must be runnable locally without additional setup beyond Node.js and npm

Requirements:

Application flow requirements:
- The application must boot successfully using `npm run start:dev`
- No additional runtime behavior is required at this stage

Architecture requirements:
- Create the project using NestJS CLI:
  - `nest new llm-agent-planner-prototype`
- Do not modify the generated module structure beyond what is necessary for this spec
- Keep the default NestJS structure intact

Persistence requirements:
- No persistence layer is required
- Do not introduce any database or ORM

LLM integration requirements:
- No LLM integration is required at this stage
- Do not create any LLM-related abstractions yet

Testing requirements:
- Keep the default Jest setup generated by NestJS
- Ensure tests still pass after changes
- Do not introduce new tests unless necessary

HTTP/API requirements:
- Keep the default NestJS controller and route (if present)
- Do not introduce new endpoints

Additional setup requirements:

Node version pinning:
- Pin Node.js version to a stable version:
  - `22.11.0`
- Create a `.nvmrc` file containing:
  - `22.11.0`
- Update `package.json` with:
  - `"engines": { "node": "22.11.0" }`

npm configuration:
- Create a `.npmrc` file with:
  - `engine-strict=true`

Environment configuration:
- Create a `.env` file with:
  - `NODE_ENV=development`
  - `PORT=3000`
  - `LLM_PROVIDER=fake`
  - `ANTHROPIC_API_KEY=`
- Create a `.env.example` file mirroring `.env`
- Ensure `.env` is ignored by git

Git ignore:
- Ensure `.gitignore` includes:
  - `node_modules`
  - `dist`
  - `.env`
  - `.env.local`
  - `coverage`

Editor configuration:
- Create a `.editorconfig` file with:
  - 2-space indentation
  - UTF-8 charset
  - LF line endings
  - trim trailing whitespace
  - final newline

Package scripts:
- Ensure `package.json` contains at least:
  - `"start": "nest start"`
  - `"start:dev": "nest start --watch"`
  - `"build": "nest build"`
  - `"test": "jest"`

README:
- Create or update `README.md` in raw Markdown with:
  - project title: `LLM Agent Planner Prototype`
  - short description explaining this is a study repository for LLM-based systems
  - setup instructions:
    - `nvm use`
    - `npm install`
    - `cp .env.example .env`
    - `npm run start:dev`

Definition of done:
- NestJS project is successfully created
- `.nvmrc` exists with pinned Node version
- `package.json` contains the `engines` field
- `.npmrc` enforces engine strict mode
- `.env` and `.env.example` are present and consistent
- `.env` is not tracked by git
- `.editorconfig` is present and correctly configured
- `.gitignore` contains required entries
- `package.json` scripts are present and working
- `README.md` exists and contains basic setup instructions
- The application runs successfully with:
  - `npm run start:dev`
- Default NestJS tests pass

Git workflow requirements:
- Create a feature branch:
  - `feat/bootstrap-project`
- Use conventional commits
- Keep commits small and focused
- Include Codex as co-author in every commit
- Use co-author format:
  - `Co-authored-by: Codex <codex@openai.com>`
  - If another Codex identity is already configured in the repo, reuse that format

Suggested commit breakdown:
- `feat(init): bootstrap nestjs project`
- `chore(node): pin node version and enforce engines`
- `chore(env): add env files and npm configuration`
- `chore(project): add editorconfig and update gitignore`
- `docs(readme): add initial project documentation`