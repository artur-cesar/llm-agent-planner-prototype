# Node.js 22.22.2 & npm 10.9.7 Migration

## Overview

This task upgrades the project runtime from Node.js `22.11.0` to `22.22.2`, and npm from `10.9.0` to `10.9.7` (if npm is explicitly pinned).

The goal is to remove compatibility friction with modern tooling while preserving strict reproducibility (`engine-strict=true`) and keeping changes minimal.

This is a Cross-Cutting Task and must not affect roadmap milestone progression.

---

## ✅ Task 1 — Update Node.js version pin

- [x] Update `.nvmrc` to `22.22.2`
- [x] Update `package.json` → `engines.node` to `22.22.2`
- [x] Search for any references to `22.11.0` in the repository
- [x] Update all relevant references to `22.22.2`

**Acceptance criteria:**
- All Node version references point to `22.22.2`
- No remaining references to `22.11.0`

---

## ✅ Task 2 — Update npm version pin (if applicable)

- [x] Check if `package.json` includes `engines.npm`
- [x] If present, update from `10.9.0` to `10.9.7`
- [x] Check for `packageManager` field
- [x] Update only if necessary for consistency

**Acceptance criteria:**
- npm is updated to `10.9.7` if previously pinned
- No unnecessary npm pinning introduced

---

## ✅ Task 3 — Clean install with new runtime

- [x] Remove `node_modules`
- [x] Remove `package-lock.json` if needed
- [x] Install dependencies using Node `22.22.2`
- [x] Ensure installation works with `engine-strict=true`

**Acceptance criteria:**
- `npm install` works without engine mismatch
- Lockfile reflects the new runtime

---

## ✅ Task 4 — Fix dependency compatibility issues

- [x] Run:
  - `npm run lint`
  - `npm run test`
  - `npm run format`
- [x] Identify failures caused by Node/npm upgrade
- [x] Fix issues with minimal changes:
  - update only required dependencies
  - remove artificial pins created for Node `22.11.0`
- [x] Avoid broad upgrades

**Acceptance criteria:**
- All scripts pass
- No unnecessary dependency churn
- Tooling works correctly under Node `22.22.2`

---

## ✅ Task 5 — Validate tooling compatibility

- [ ] Verify ESLint runs without errors
- [ ] Verify Jest runs correctly
- [ ] Verify Husky hooks still work
- [ ] Verify TypeScript compiles successfully

**Acceptance criteria:**
- All tooling works as expected
- No regressions introduced

---

## ✅ Task 6 — Update documentation

- [ ] Update `README.md`:
  - Node version → `22.22.2`
  - npm version → `10.9.7` (if relevant)
- [ ] Ensure setup instructions are accurate

**Acceptance criteria:**
- README reflects correct versions
- Instructions are clear and minimal

---

## ✅ Task 7 — Final validation

- [ ] Run:
  - `npm install`
  - `npm run start:dev`
  - `npm run lint`
  - `npm run test`
- [ ] Validate application starts correctly
- [ ] Ensure no regressions

**Acceptance criteria:**
- Project runs successfully under Node `22.22.2`
- All scripts pass
- No unexpected behavior changes

---

## 🧾 Commit Strategy

Each task should result in one commit:

- chore(node): upgrade node version to 22.22.2
- chore(npm): upgrade npm version to 10.9.7 (if pinned)
- chore(deps): reinstall dependencies under node 22.22.2
- chore(deps): fix compatibility issues after node upgrade
- chore(tooling): validate tooling under new runtime
- docs(readme): update node and npm versions

Each commit must include:

Co-authored-by: OpenAI Codex <codex@openai.com>
